name: isolate_workgroup
description: A Dart package for managing a dynamic group of isolates — supports
  one-off job dispatch, persistent members, health monitoring, and runtime
  workgroup resizing.
version: 1.0.0

homepage: https://github.com/definitelyme/isolate_workgroup
repository: https://github.com/definitelyme/isolate_workgroup
issue_tracker: https://github.com/definitelyme/isolate_workgroup/issues

topics:
  - isolate
  - concurrency
  - parallel
  - worker-pool
  - compute

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  meta: ^1.16.0

dev_dependencies:
  test:
  lints:
